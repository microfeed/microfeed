# microfeed classic

This is the frozen pre-versioned microfeed design. It exists for appearance-preserving upgrades and as the internal emergency fallback. Start new theme development from `themes/default` or `microfeed-theme init` instead of modifying this package.
